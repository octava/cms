{% extends admin.getTemplate('base_list_field') %}

{% block field %}
    {% if field_description.options.add_lang in object.translations|keys %}
        {{ object.translations[field_description.options.add_lang] }}
    {% endif %}
{% endblock %}