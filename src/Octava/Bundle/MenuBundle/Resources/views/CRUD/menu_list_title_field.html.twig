{% spaceless %}
    <td class="sonata-ba-list-field sonata-ba-list-field-{{ field_description.type }}"
        objectId="{{ admin.id(object) }}"
        style="padding-left: {{ (object.level+1) * 30 }}px;{{ object.level == 1 ? 'font-weight:bold;' : '' }}">
        {{ value }}
    </td>
{% endspaceless %}
