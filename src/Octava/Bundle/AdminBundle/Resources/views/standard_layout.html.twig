{% extends 'SonataAdminBundle::standard_layout.html.twig' %}
{% set _currentAdminRoute = '' %}
{% if _currentAdminRoute is empty %}
    {% set _currentAdminRoute = currentAdmin.baseRouteName|default('') %}
{% endif %}

{% block stylesheets %}
    {{ parent() }}

    <link rel="stylesheet" href="{{ asset('bundles/octavaadmin/css/octava_layout.css') }}">
{% endblock %}